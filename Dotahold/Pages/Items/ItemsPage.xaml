<Page
    x:Class="Dotahold.Pages.Items.ItemsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Dotahold.Pages.Items"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:models="using:Dotahold.Models"
    xmlns:converters="using:Dotahold.Converters"
    mc:Ignorable="d">

    <Page.Resources>
        <converters:ItemTierToColorConverter x:Key="ItemTierToColorConverter"/>
        <converters:IntToVisibilityConverter x:Key="IntToVisibilityConverter"/>
        <converters:StringArrayToStringConverter x:Key="StringArrayToStringConverter"/>
        <converters:EmptyToVisibilityConverter x:Key="EmptyToVisibilityConverter"/>
    </Page.Resources>

    <Grid HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" MaxWidth="360"/>
            <ColumnDefinition Width="2*"/>
        </Grid.ColumnDefinitions>
        <Grid Grid.Column="0" BorderThickness="0,0,1,0" BorderBrush="{ThemeResource DotaholdSeparatorColor}" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <TextBlock Grid.Row="0" Margin="32,36,0,8" Text="Items" FontFamily="{ThemeResource ReaverBold}" FontSize="28"/>
            <Grid Grid.Row="1" Margin="20,0" HorizontalAlignment="Stretch">
                <TextBox PlaceholderText="Search items" FontSize="14" Padding="30,6,4,0" Height="34" TextChanged="TextBox_TextChanged" HorizontalAlignment="Stretch" VerticalAlignment="Center"/>
                <FontIcon FontFamily="{ThemeResource SymbolThemeFontFamily}" Glyph="&#xE094;" Opacity="0.7" FontSize="14" Margin="10,0,0,0" HorizontalAlignment="Left" VerticalAlignment="Center" IsHitTestVisible="False"/>
            </Grid>
            <ListView Grid.Row="2" ItemsSource="{x:Bind _viewModel.ItemsViewModel.Items,Mode=OneWay}" 
                      SelectedItem="{x:Bind _viewModel.ItemsViewModel.SelectedItem,Mode=OneWay}" 
                      SelectionChanged="ListView_SelectionChanged" Padding="4,8,4,8" HorizontalContentAlignment="Stretch" HorizontalAlignment="Stretch">
                <ListView.ItemTemplate>
                    <DataTemplate x:DataType="models:ItemModel">
                        <Grid Height="64">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Grid Height="42" Width="56" CornerRadius="4" Background="{ThemeResource DotaholdPlaceholderBackgroundColor}">
                                <Image Height="42" Width="56" Stretch="UniformToFill" Source="{x:Bind ItemImage.Image,Mode=OneWay}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Grid>
                            <TextBlock Grid.Column="1" FontWeight="Medium" FontSize="16" Margin="16,0" TextTrimming="CharacterEllipsis"
                                       Text="{x:Bind DotaItemAttributes.dname}" ToolTipService.ToolTip="{x:Bind DotaItemAttributes.dname}"
                                       HorizontalAlignment="Left" VerticalAlignment="Center"/>
                        </Grid>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Grid>

        <Grid Grid.Column="1" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
            <ScrollViewer HorizontalAlignment="Stretch" VerticalAlignment="Stretch" HorizontalScrollMode="Disabled" VerticalScrollMode="Auto" VerticalScrollBarVisibility="Auto">
                <StackPanel Orientation="Vertical" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="64,42">

                    <StackPanel Margin="0,0,0,12" Spacing="12" Orientation="Horizontal">
                        <!--Image-->
                        <Grid Height="62" Width="85" CornerRadius="8" Background="{ThemeResource DotaholdPlaceholderBackgroundColor}">
                            <Image Height="62" Width="85" Stretch="UniformToFill" Source="{x:Bind _viewModel.ItemsViewModel.SelectedItem.ItemImage.Image,Mode=OneWay}"/>
                        </Grid>
                        <StackPanel Spacing="6" Orientation="Vertical" HorizontalAlignment="Left" VerticalAlignment="Center">
                            <!--Name-->
                            <TextBlock Text="{x:Bind _viewModel.ItemsViewModel.SelectedItem.DotaItemAttributes.dname,Mode=OneWay}" FontFamily="{ThemeResource ReaverBold}" FontSize="22" CharacterSpacing="42"/>
                            <StackPanel Orientation="Horizontal">
                                <!--Price-->
                                <StackPanel Orientation="Horizontal" Spacing="8" Padding="0,0,16,0" Visibility="{x:Bind _viewModel.ItemsViewModel.SelectedItem.DotaItemAttributes.cost,Converter={StaticResource IntToVisibilityConverter},Mode=OneWay}">
                                    <Image Width="25" Height="17" Source="/Assets/Items/icon_gold.png" HorizontalAlignment="Left" VerticalAlignment="Center"/>
                                    <TextBlock Text="{x:Bind _viewModel.ItemsViewModel.SelectedItem.DotaItemAttributes.cost,Mode=OneWay}"
                                               FontSize="16" FontFamily="{ThemeResource RadianceSemiBold}" Opacity="0.7" VerticalAlignment="Center"/>
                                </StackPanel>
                                <!--Tier-->
                                <StackPanel Orientation="Horizontal" Padding="8,3,8,2" CornerRadius="2" Background="{x:Bind _viewModel.ItemsViewModel.SelectedItem.DotaItemAttributes.tier,Converter={StaticResource ItemTierToColorConverter},Mode=OneWay}" 
                                            Visibility="{x:Bind _viewModel.ItemsViewModel.SelectedItem.DotaItemAttributes.tier,Converter={StaticResource IntToVisibilityConverter},Mode=OneWay}">
                                    <TextBlock Text="Neutral T" FontFamily="{ThemeResource RadianceSemiBold}" FontSize="12" Foreground="#99FFFFFF" VerticalAlignment="Center"/>
                                    <TextBlock Text="{x:Bind _viewModel.ItemsViewModel.SelectedItem.DotaItemAttributes.tier,Mode=OneWay}" FontFamily="{ThemeResource RadianceSemiBold}" FontSize="12" Foreground="#99FFFFFF" VerticalAlignment="Center"/>
                                </StackPanel>
                            </StackPanel>
                        </StackPanel>
                    </StackPanel>

                    <TextBlock Text="{x:Bind _viewModel.ItemsViewModel.SelectedItem.DotaItemAttributes.hint,Mode=OneWay,Converter={StaticResource StringArrayToStringConverter}}"
                               Visibility="{x:Bind _viewModel.ItemsViewModel.SelectedItem.DotaItemAttributes.hint,Mode=OneWay,Converter={StaticResource EmptyToVisibilityConverter}}"
                               Margin="0,4" TextWrapping="WrapWholeWords" Opacity="0.8" CharacterSpacing="42"/>

                    <TextBlock Text="{x:Bind _viewModel.ItemsViewModel.SelectedItem.DotaItemAttributes.notes,Mode=OneWay}"
                               Visibility="{x:Bind _viewModel.ItemsViewModel.SelectedItem.DotaItemAttributes.notes,Mode=OneWay,Converter={StaticResource EmptyToVisibilityConverter}}"
                               Margin="0,4" TextWrapping="WrapWholeWords" Foreground="ForestGreen" CharacterSpacing="42"/>

                    <!--<TextBlock Text="{x:Bind sAttribInfo,Mode=OneWay}"
                           TextWrapping="WrapWholeWords"
                           CharacterSpacing="40"
                           Margin="0,4"
                           Visibility="{x:Bind sAttribInfo,Converter={StaticResource Str2Vis},Mode=OneWay}"/>
                    <StackPanel Orientation="Horizontal" Margin="6">
                        <Border Height="14" Width="14" CornerRadius="3" BorderBrush="#10162c" BorderThickness="1"
                            Visibility="{x:Bind ViewModel.CurrentItem.mc,Converter={StaticResource Int2Vis},Mode=OneWay}">
                            <Border.Background>
                                <ImageBrush Stretch="UniformToFill" ImageSource="/Assets/Icons/Data/icon_mana.png"/>
                            </Border.Background>
                        </Border>
                        <TextBlock Text="{x:Bind ViewModel.CurrentItem.mc,Mode=OneWay}"
                               Margin="4,0,12,0" Visibility="{x:Bind ViewModel.CurrentItem.mc,Converter={StaticResource Int2Vis},Mode=OneWay}"/>
                        <Border Height="14" Width="14" CornerRadius="3" BorderBrush="#10162c" BorderThickness="1"
                            Visibility="{x:Bind ViewModel.CurrentItem.cd,Converter={StaticResource Int2Vis},Mode=OneWay}">
                            <Border.Background>
                                <ImageBrush Stretch="UniformToFill" ImageSource="/Assets/Icons/Data/icon_cooldown.png"/>
                            </Border.Background>
                        </Border>
                        <TextBlock Text="{x:Bind ViewModel.CurrentItem.cd,Mode=OneWay}"
                               Margin="4,0" Visibility="{x:Bind ViewModel.CurrentItem.cd,Converter={StaticResource Int2Vis},Mode=OneWay}"/>
                    </StackPanel>
                    <Grid x:Name="BackgroundGrid" Background="#08FFFFFF" CornerRadius="6"
                      Visibility="{x:Bind ViewModel.CurrentItem.lore,Converter={StaticResource Str2Vis},Mode=OneWay}">
                        <TextBlock Text="{x:Bind ViewModel.CurrentItem.lore,Mode=OneWay}"
                               Opacity="0.6" Margin="12,10" FontSize="13" FontStyle="Italic" CharacterSpacing="20" TextWrapping="WrapWholeWords"/>
                    </Grid>
                    <TextBlock Text="Recipe" FontWeight="Medium" CharacterSpacing="20" FontSize="15" Margin="4,18,0,4"
                           Visibility="{x:Bind bHasComponents,Mode=OneWay}"/>
                    <GridView x:Name="ComponentsGridView" ItemsSource="{x:Bind vComponentsList,Mode=OneWay}" SelectionMode="None" 
                          IsItemClickEnabled="True" ItemClick="OnClickComponents"
                          Visibility="{x:Bind bHasComponents,Mode=OneWay}">
                        <GridView.ItemTemplate>
                            <DataTemplate x:DataType="coremodel:DotaItemModel">
                                <Grid Height="34" Width="45" Margin="5,0" CornerRadius="6" ToolTipService.ToolTip="{x:Bind dname}">
                                    <Grid.Background>
                                        <ImageBrush Stretch="UniformToFill" ImageSource="{x:Bind ImageSource,Mode=OneWay}"/>
                                    </Grid.Background>
                                </Grid>
                            </DataTemplate>
                        </GridView.ItemTemplate>
                    </GridView>-->
                </StackPanel>
            </ScrollViewer>
        </Grid>
    </Grid>
</Page>
