<Page
    x:Class="Dotahold.Views.MatchPlayerPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Dotahold.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    xmlns:cvt="using:Dotahold.Converters"
    xmlns:ui="using:Dotahold.UI" 
    xmlns:mdl="using:Dotahold.Models" 
    xmlns:controls="using:Microsoft.Toolkit.Uwp.UI.Controls" 
    xmlns:muxc="using:Microsoft.UI.Xaml.Controls"
    NavigationCacheMode="Required" Background="Transparent">

    <Grid>
        <ScrollViewer x:Name="PlayerInfoScrollViewer" Padding="0,148,0,0">
            <StackPanel>
                <!--物品购买记录-->
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" Margin="26,16,0,4"
                            Visibility="{x:Bind ViewModel.CurrentMatchPlayer.vPurchaseLog.Count,Mode=OneWay,Converter={StaticResource Int2Vis}}">
                    <FontIcon Glyph="&#xE7BF;" Margin="0,0,8,0" FontSize="20"/>
                    <TextBlock Text="Purchase" FontWeight="Bold" Margin="0,6" FontSize="16" FontFamily="/Fonts/Reaver-Bold.ttf#Reaver" CharacterSpacing="20"/>
                </StackPanel>
                <ScrollViewer Padding="0,0,0,0" HorizontalScrollMode="Auto" VerticalScrollMode="Disabled" HorizontalScrollBarVisibility="Hidden" VerticalScrollBarVisibility="Disabled"
                              Visibility="{x:Bind ViewModel.CurrentMatchPlayer.vPurchaseLog.Count,Mode=OneWay,Converter={StaticResource Int2Vis}}">
                    <muxc:ItemsRepeater ItemsSource="{x:Bind ViewModel.CurrentMatchPlayer.vPurchaseLog,Mode=OneWay}" Margin="24,0,12,0">
                        <muxc:ItemsRepeater.Layout>
                            <muxc:StackLayout Orientation="Horizontal" Spacing="0"/>
                        </muxc:ItemsRepeater.Layout>
                        <DataTemplate x:DataType="mdl:Purchase_Log">
                            <Grid Visibility="{x:Bind key,Converter={StaticResource Null2Vis}}">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="*"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>
                                <Rectangle Grid.Row="0" Height="1" Width="6" HorizontalAlignment="Left" VerticalAlignment="Center" Fill="{ThemeResource MATCHPLAYER_DATA_BK}"/>
                                <Rectangle Grid.Row="0" Height="1" Width="6" HorizontalAlignment="Right" VerticalAlignment="Center" Fill="{ThemeResource MATCHPLAYER_DATA_BK}"/>
                                <Border Grid.Row="0" CornerRadius="8" Margin="8,0" HorizontalAlignment="Center" VerticalAlignment="Center">
                                    <Image Width="44" Height="32" VerticalAlignment="Center" Source="{x:Bind ItemImageSource,Mode=OneWay}"/>
                                </Border>
                                <TextBlock Grid.Row="1" Text="{x:Bind PurchaseTime,Mode=OneWay}" FontSize="12.5" Opacity="0.5" Margin="0,4,0,0" FontWeight="Medium" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Grid>
                        </DataTemplate>
                    </muxc:ItemsRepeater>
                </ScrollViewer>

                <!--Benchmarks-->
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" Margin="26,16,0,4"
                            Visibility="{x:Bind ViewModel.CurrentMatchPlayer.vBenchmarks.Count,Mode=OneWay,Converter={StaticResource Int2Vis}}">
                    <FontIcon Glyph="&#xED5E;" Margin="0,0,8,0" FontSize="20"/>
                    <TextBlock Text="Benchmarks" FontWeight="Bold" Margin="0,6" FontSize="16" FontFamily="/Fonts/Reaver-Bold.ttf#Reaver" CharacterSpacing="20"/>
                </StackPanel>
                <ListView HorizontalContentAlignment="Stretch" Margin="22,0,22,12"
                          SelectionMode="None" IsItemClickEnabled="False"
                          ItemsSource="{x:Bind ViewModel.CurrentMatchPlayer.vBenchmarks,Mode=OneWay}"
                          Visibility="{x:Bind ViewModel.CurrentMatchPlayer.vBenchmarks.Count,Mode=OneWay,Converter={StaticResource Int2Vis}}">
                    <ListView.ItemsPanel>
                        <ItemsPanelTemplate>
                            <controls:WrapPanel HorizontalSpacing="0" VerticalSpacing="0"/>
                        </ItemsPanelTemplate>
                    </ListView.ItemsPanel>
                    <ListView.ItemTemplate>
                        <DataTemplate x:DataType="mdl:Benchmark">
                            <Grid CornerRadius="4" Margin="-10,0,-10,8" MinWidth="120" Padding="10,6,10,6" Background="{ThemeResource MATCHPLAYER_DATA_BK}">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>
                                <TextBlock FontSize="12" Opacity="0.7" Margin="4,0,4,0" FontWeight="Medium" HorizontalAlignment="Left" VerticalAlignment="Center">
                                    <Run Text="{x:Bind Name}"/>
                                    <Run Text=":"/>
                                    <Run Text="{x:Bind raw}" Foreground="{x:Bind pct,Converter={StaticResource Benchmark2Color}}"/>
                                </TextBlock>
                                <Grid Grid.Row="1">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <Border Margin="4,4,2,0" HorizontalAlignment="Left" VerticalAlignment="Center" Height="8" Width="120" CornerRadius="3" Background="{ThemeResource MATCHPLAYER_DATA_BK}">
                                        <Border HorizontalAlignment="Left" Height="8" Width="{x:Bind BarWidth}" CornerRadius="3" Background="{x:Bind pct,Converter={StaticResource Benchmark2Color}}"/>
                                    </Border>
                                    <TextBlock Grid.Column="1" FontSize="12" Opacity="0.5" Margin="4,0,2,-1" FontWeight="Medium" HorizontalAlignment="Left" VerticalAlignment="Center">
                                        <Run Text="{x:Bind pct}"/>
                                        <Run Text="%"/>
                                    </TextBlock>
                                </Grid>
                            </Grid>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>
